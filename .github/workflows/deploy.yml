name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22 # Ini akan memaksa GitHub menggunakan Node versi 22

      - name: Install dependencies
        run: npm install

      - name: Build and upload with Astro
        uses: withastro/action@v2